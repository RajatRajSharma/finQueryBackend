# --- Secrets ---
.env

# --- Python ---
venv/
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# --- Data (raw PDFs ARE committed) ---
# The eval question SET is committed (it's the test fixture); only cached run
# results + the saved baseline stay local.
data/eval/results.json
data/eval/baseline.json

# --- Qdrant local storage (if mounted) ---
qdrant_storage/

# --- IDE / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db
